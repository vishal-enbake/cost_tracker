<h1>Listing receipts</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @receipts.each do |receipt| %>
  <tr>
    <td><%= receipt.name %></td>
    <td><%= link_to 'Show', [@division, receipt] %></td>
    <td><%= link_to 'Edit', edit_division_receipt_path(@division, receipt) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Receipt', new_division_receipt_path(@division) %>
