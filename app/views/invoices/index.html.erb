<h1>Listing invoices</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Amount</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @invoices.each do |invoice| %>
  <tr>
    <td><%= invoice.name %></td>
    <td><%= invoice.amount %></td>
    <td><%= link_to 'Show', [@division, invoice] %></td>
    <td><%= link_to 'Edit', edit_division_invoice_path(@division, invoice) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Invoice', new_division_invoice_path(@division) %>
